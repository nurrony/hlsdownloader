/* istanbul ignore file */
import { isValidPlaylist, isValidUrl, omit, parseUrl, stripFirstSlash, isNotFunction } from './utils';

/**
 * Utility module for HLSDownloader
 * @module HLSDownloaderUtils
 * @name HLSDownloaderUtils
 * @description
 * <header><h1>HLSDownloaderUtils</h1></header>
 * <p>Utility module for HLSDownloader</p>
 */
export default { isValidPlaylist, isValidUrl, omit, parseUrl, stripFirstSlash, isNotFunction };
